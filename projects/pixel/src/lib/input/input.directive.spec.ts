import { PixelInputDirective } from './input.directive';

describe('InputDirective', () => {
  it('should create an instance', () => {
    const directive = new PixelInputDirective();
    expect(directive).toBeTruthy();
  });
});
